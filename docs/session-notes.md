# Session notes

Short lived context that does not belong in scope, a spec, or AGENTS.md yet.
Written by /checkpoint save, read by /checkpoint restore.

## Open threads

- `docs/archive/jobhunt-carry-forward.md` (moved into `docs/archive/` by pull request 123) gained about 67 lines when PR 14 merged on 2026-08-26, content that had lived only on this machine, on a branch that had never been pushed. It covers untrusted job descriptions reaching a model, stale postings from ATS feeds, legitimacy as a signal kept out of the score, a two tier ranking idea marked as unproven, and verifying a rendered PDF's text layer. Still open for feature 14's own design pass; the text these concerns worry about first enters at feature 11 (spec 0013, now written), but feeding it to a model stays exclusively feature 14's concern. **The audit on 2026-09-02 found the feature 4 entry this note recorded as spent is still live**: the key format landed in spec 0001 (index line 38) and spec 0002 (index lines 191-192) for the env vars, but its row level security line ("key rotation is not a substitute for RLS") appears in no spec at all. **The "Features 5 and 11" entry is now resolved**: spec 0013 (written 2026-09-04) verified the Jobsworth attribution and both link targets directly against Adzuna's own terms of service, rather than trusting this note's secondhand wording, and recorded them in AC-6 and AC-7. **The logo asset half is now closed as well** (feature 11 shipped 2026-09-04): the engineer supplied `src/features/search/adzuna-logo.svg`, spec 0005's own Follow-up is ticked, and its design table row naming the wrong link targets was corrected in the same pass. **What is still live in this entry is only the feature 4 part above**, the row level security line that appears in no spec. **Root `AGENTS.md` now carries it** ("A key is not a policy", added by `635ab4e` in pull request 123), but it is still in no spec, which is the only reason this entry stays.
- GitGuardian Security Checks reported `skipping` on every run against pull request 18, then `pass` on pull requests 19 and 20. Nobody changed its configuration, so why it started running is unknown. It matters because pull request 18's branch history was rewritten specifically to remove a Supabase secret key that push protection caught, and a scanner that silently skips is not a scanner. Worth understanding before anyone leans on it. It passed on pull requests 19, 20, 40 and 41, which made it look confined to pull request 18. **That reading is now wrong: it reported `skipping` again on pull request 92 (feature 11), merged 2026-09-04.** So it is intermittent after all, and it skipped on the one branch that changed credential redaction in two places and removed two NUL bytes from the assertion guarding a fixture against committing a live key. Still unexplained, and now worth more than curiosity. **It reported `pass` on pull request 94 (feature 12, merged 2026-09-05)**, so the pattern is still intermittent with no visible cause: skip on 18 and 92, pass on 19, 20, 40, 41 and 94.
- Feature 4's `Test it` box is partly satisfied already and the box does not say so. Feature 8's integration tests prove `public.profile`'s row level security in both directions, and **feature 9 (pull request 82, merged 2026-09-02) added the same kind of evidence for three more tables**: `profile_skill`, `work_experience` and `job_preference`, via two real accounts writing through the running app and each reading back only their own rows, plus a spoofed update and a spoofed delete that each change zero rows and are reported as named failures (`test/integration/profile-form.test.ts` and `profile-actions.test.ts`). **Feature 12 (built 2026-09-05) added the same kind of evidence for `application`**: `test/integration/application-actions.test.ts` has two real accounts, and the second cannot list, mark or remove the first's row while the first's row survives the attempt, plus the unique constraint and the foreign key each refused for real. **That leaves `application_answer` as the only one of the six with no coverage at all**, and it stays that way until feature 20 writes to it. Whoever runs `/test data model` should start from those files plus `test/integration/isolation.test.ts` and `fixtures.test.ts`, not from nothing. Left unticked on purpose: partial evidence is not evidence.
- **Spec 0006's `verify.md` line 17 is still unticked and the check has passed.** The production unfurl repeat was run successfully on `usejobhunt.dev` on 2026-08-30. It belongs to `/check verify`'s file, so `/checkpoint` leaves it alone, but somebody should tick it with the evidence before it reads as never run.
- The second UptimeRobot monitor, on `usejobhunt.vercel.app`, needs its expected status codes inverted so `200` counts as down and `3xx` as up. As configured it reports Up whether that host redirects or serves the application, so it cannot detect the one thing it was added for: the redirect breaking, which spec 0007 AC-4 leans on. The limitation is written in `docs/observability/README.md`; the fix is not applied.
- CI warns that `pnpm/action-setup@v4` and `supabase/setup-cli@v1` still target Node 20 and are being forced onto Node 24. Only a warning today, and it predates feature 8, but both CI jobs break when GitHub stops forcing. It sits in no scope row and nobody owns it.
- **Work history dates: one month and year picker instead of two separate `Select` controls** (feature 9, raised after pull request 82 merged, not a bug). The pair works and is accessible, it just reads as two controls for one fact. A full calendar style picker would be wrong here, and the reason is the schema rather than taste: `work_experience.started_on` and `ended_on` are `date` columns whose **day is pinned to 1 by a check constraint** (`supabase/migrations/20260825162457_data_model.sql`, lines 112 and 115), so a day level control would offer precision the database refuses to store. No month and year component exists in `src/components/ui` today, so this needs a component decision before it is built. Route through `/architect`.
- **Work history: keep the other entries visible while adding, editing or deleting one** (feature 9, raised after pull request 82 merged, not a bug). `ExperienceSection` in `src/features/profile/experience-section.tsx` returns exactly one of the list, the add form, the edit form or the delete confirmation per URL state, so the rest of somebody's history disappears while they work on a single entry. The one path that already renders a form state next to the list is the entry not found fallback. This does **not** reopen spec 0010's decision that every edit is a full navigation rather than a client side toggle: it is the same server rendered per URL model, just rendering more in that one response.
- **Skills: a chip list instead of one skill per line in a textarea** (feature 9, raised after pull request 82 merged, not a bug). Type a skill, press Enter, it becomes a removable chip in a growing list. **The open question to settle when this is scoped**: whether it also applies to `desired_titles` and `desired_locations` in search preferences, which use the identical newline per value parsing today (all three call `newlineList` in `src/features/profile/schemas.ts`, at lines 138, 175 and 181). Changing skills alone would leave three fields that behave identically today behaving differently, which is fine only if it is deliberate. Route through `/architect` for that scope decision.
- **`ExperienceSection` renders the entry not found state from two identical copies**, and one of them should be deleted (`src/features/profile/experience-section.tsx`). The `EntryGone` component at line 137 is called only from the `gone` branch at line 54; the `entry === undefined` branch at lines 74 to 83 still holds a byte for byte duplicate of the same markup. **No behaviour is wrong and every test passes**, because both copies render the same thing, which is why nothing caught it. It is worth fixing anyway: `EntryGone` was extracted during `/debug` on 2026-09-02 precisely so those two paths could not drift apart, and leaving both defeats that. The fix is deleting lines 74 to 83 in favour of `return <EntryGone entries={entries} />`. **How it survived is the part worth knowing**: the `/debug` edit that was meant to replace the inline copy matched against text Prettier had since reformatted, so it silently did nothing, and the `/debug` report then claimed the two paths had been unified. Three edits failed the same way in that session. When editing by string match, check the replacement actually landed rather than trusting the exit code.
- **The CI environment variable gap, raised 2026-09-06, deliberately not yet a reflex.** `docs/reflexes.md`'s existing rule (added 2026-09-04) about a new `src/env.ts` variable covers only that it needs setting in every Vercel environment; it says nothing about `.github/workflows/ci.yml`'s own placeholder env blocks. Feature 11 added its two Adzuna keys to both Vercel and both CI test job env blocks. Feature 13 added its two vendor keys to Vercel per that reflex and missed CI: `/check review`'s round 1 caught it as a blocker before the pull request opened, since neither test job passed `OPENAI_API_KEY` nor `GOOGLE_GENERATIVE_AI_API_KEY`, and both would have failed the moment CI ran. Two instances now, the second one caught before merge rather than after, which is why the engineer chose to wait for a third instance rather than write the rule off two. Recorded here so it is not re-raised as new, and so a third instance is recognised as the trigger to write it up in `docs/reflexes.md`.
- **A fresh local database has no profile data at all, and `pnpm db:reset` seeds none.** On 2026-09-07 every `profile_skill` and `work_experience` table was empty, so every caller hit spec 0015's AC-7 thin profile gate and nothing could be scored, which looks exactly like a broken feature until you check the rows. `dev-one@example.test` was seeded by hand with 6 skills and 2 roles and that data is still there. Anyone verifying scoring on a reset stack has to seed a profile first; worth deciding whether the seed should carry one rather than leaving each session to rediscover this.

- **AC-17's "brought into view" clause is flaky rather than proven or failed.** `/check verify` on 2026-09-07 drove the restore four times against the real app: three times the browser scrolled the restored control into view (`scrollY` 4406 to 502, and 4631 to 6367, both stable when sampled at 120ms, 1s and 3s), and once it did not, leaving the control at `top: 1359` in a 953px viewport with `scrollY` unmoved from where the Tab walk had left it. Re-running that exact script passed. **A fifth run, driven by the engineer in a real browser later the same day, passed**: a card tabbed into near the bottom of the pending list, ranked into a top position by the reveal, kept focus on its own `View the posting` link and was scrolled into view. So the count is now four passes out of five, and `verify.md`'s step is ticked on that basis rather than left reading as never run. **The single miss is still unexplained and has never been reproduced**, including by re-running its own script immediately afterwards, so this entry stays open rather than closing with the tick. Worth chasing whether layout settling after the reveal can push the control back out of view, because the criterion it serves (WCAG 2.2, Focus Not Obscured) does not tolerate a one in five miss either.
- **Twenty one local branches are merged into `main` and never deleted.** `gh pr merge` was run without `--delete-branch` for most of them, and the remote copies were still there too. Harmless, but the list is now long enough to hide a genuinely unmerged branch. **The practice started on 2026-09-08**: pull request 112 was merged with `--delete-branch`, so its branch is gone from both local and origin, and `origin` now carries only `main` plus whatever is unpushed. The backlog of twenty one local branches is unchanged and still wants one cleanup pass. **The practice held across five more merges on 2026-09-08** (pull requests 113 through 117, all merged with `--delete-branch`), and `origin` still carries only `main`, so the twenty one are purely historical now rather than a growing list. Counted again at the end of 2026-09-08: `git branch --merged main` still returns twenty one besides `main`. **Measured again on 2026-09-09 and the backlog is gone**: `git branch` returns `main` alone, `git branch --merged main` returns only `main`, and `git ls-remote --heads origin` returns a single head. Pull requests 118 and 119 were both merged with `--delete-branch` that day and left nothing behind. Nothing in this session deleted the twenty one, so they were cleaned up outside it. **The backlog resolved, and then reopened by exactly one on 2026-09-10**: pull request 121 (feature 17) was merged with a plain `gh pr merge 121 --merge`, without `--delete-branch`, so `feat/cross-vendor-self-check` is still a local branch merged into `main`. `origin` is clean, carrying only `main`, because the remote copy was removed on merge without anyone asking. So the lapse is local only and the fix is one `git branch -d feat/cross-vendor-self-check`. Kept rather than deleted for that reason: as a backlog of twenty one it is gone, but the practice that created it slipped again the first time a merge ran without the flag, and an entry deleted now would let it regrow unwatched. **Clean again on 2026-09-11**: `feat/cross-vendor-self-check`, `docs/archive-planning-docs` and `docs/readme-screenshots` were each deleted with `git branch -d`, and `git branch --merged main` returns only `main`. Why only the local side ever piles up: GitHub removes the remote branch on merge even without `--delete-branch`, seen on pull requests 121 through 124 (124 was merged with a plain `--merge`). Pull requests 125 and 127 were merged with `--delete-branch` and left nothing behind; `docs/result-card-teichert` (pull request 126, merged without the flag) is the one local leftover.
- **A plain `git rebase origin/main` reported a failure that was not one, on 2026-09-08.** Rebasing `feat/eval-harness-runner`'s 11 commits onto two docs only commits that shared no files with it, git stopped at `docs(scope): mark feature 16's build milestones done` with `Could not apply ... It has been rescheduled`, while `git diff --diff-filter=U` was empty and the working tree was clean. The rebase todo had picked up duplicate entries (`ad87617` appeared twice in `.git/rebase-merge/done`) and git ran 13 steps for 11 commits. A single `git rebase --continue` finished it and the result was correct, verified three ways: 11 commits with no duplicated subjects, the only tree difference against the pre rebase tip being the three docs files that came in, and typecheck, lint and 1162 tests green. Cause unknown, no `rebase.*` or `sequence.*` config is set. Worth knowing that "Successfully rebased" on top of a todo list with duplicates is not by itself evidence the history is intact. **A second instance would make this a reflex**, the same way the CI environment variable entry above is waiting on a third.
- **Five reflexes now share one shape, "this signal cannot tell a pass from a broken instrument"**: 2026-09-05 (make a number move before trusting a before and after reading), 2026-09-05 (delete the build output before comparing a generated artifact), 2026-09-06 (`gh pr checks` reads as done when it is not), 2026-09-07 (name the scope a check ran over) and 2026-09-07 (the shell fallback that prints a reassuring word). Each has a distinct mechanism and a distinct fix, so none is a duplicate, but they are a consolidation candidate whenever `docs/reflexes.md` next gets a pruning pass. Raised during the 2026-09-07 capture at 34 rules. **Now six, and the file is at 35 rules.** The 2026-09-09 rule (a value that answers an adjacent question) was filed beside these five on purpose rather than merged into them, because the instrument there is working correctly. That makes `/reflex audit` the next pass, and whether the six collapse into fewer is the decision that pass exists to make.
- **One case from the 2026-09-08 batch of green signal over a defect still has no home.** The other five were captured on 2026-09-09: three (the eval report's `summary` denominator, its `pairs` array against the top level `skipped` array, and `gh run list --limit 1` naming a different workflow) became the new rule in `docs/reflexes.md`, and two were already rules, the requirements sentence test inside the 2026-09-07 scope rule and the unquoted `--include=*.ts` inside the 2026-09-07 `|| echo` rule. What is left is a background poller that died on its first line because `status` is a read only variable in zsh, so the check never ran and produced nothing rather than reporting the wrong thing. One incident, a different shape from the rest, and not yet worth a rule on its own.
- **The only real measurements of the scorer live in a gitignored directory.** The two full sixteen pair runs on 2026-09-08 are the entire evidence base for how `BAND_ANCHORS` actually behaves, and they sit in `test/eval/.output/`, which `.gitignore` covers deliberately. Spec 0018 copied the numbers it needed into its own `verify.md` for exactly this reason, but the full per pair data goes away on a clean checkout or a new machine, and reproducing it costs 160 vendor calls. Worth deciding whether a run's report should be committable, perhaps a small curated one per anchor hash rather than every run, so a later reader can check a claim without paying to regenerate the evidence.
- **Auto merge is disabled on this repository.** `gh pr merge <n> --auto` fails with `GraphQL: Auto merge is not allowed for this repository (enablePullRequestAutoMerge)`, confirmed on pull request 119 on 2026-09-09, so a pull request cannot be queued to land when its checks go green. The working pattern is to poll the per check list with `rtk proxy gh pr checks <n>` until nothing reads `pending`, confirm no row failed, and only then merge. This is a durable fact about the repository rather than session residue, so it probably belongs in root `AGENTS.md`'s Git block; `/sync` owns that file and this skill may not write it. **Checked on 2026-09-10 and NOT aged out.** The pattern was used again on pull request 121 and worked exactly as described: `rtk proxy gh pr checks 121` showed three checks `pending`, then all `pass` with `Apply migrations (production)` reading `skipping`, and the merge went through afterwards. `AGENTS.md`'s Git block still says nothing about any of it, so this has found no home in scope, a spec or `docs/reflexes.md` and stays here until `/sync` moves it.
- **`test/integration/landing-rule.test.ts` fails intermittently at roughly 13%, and it belongs to spec 0008 rather than to feature 17.** Two failures in 15 full suite runs, reproduced on run 6 of ten captured runs on 2026-09-10; the file is not in feature 17's diff. The failing case is "sends a user with no profile row to /profile", which received `/search`. **Cause**: a transient `hasProfileRow()` failure, which `src/lib/landing-rule.ts` deliberately maps to `/search` under AC-7a (an errored read is not an empty profile), so the assertion cannot tell a real `/search` from a fallback one and only notices because the expected value happens to be the other. It does not reproduce in isolation, 0 failures in 10 runs of that file alone, which points at contention under full suite load. **The `usage_cap` race hypothesis was disproven, by runtime per file timing rather than by reading `vitest.config.mts`**: across five runs the `integration` project always finished before the first `integration-serial` file started (gap 506 to 600 ms), no two serial files overlapped, and the two cap mutating files never overlapped. Worth knowing that the serial file ORDER varies between runs even though execution stays sequential, so a serial test that depended on running before a sibling would be flaky. **Whether feature 17's branch made it worse is unsettled and the experiment was underpowered**: six runs excluding that branch's integration files gave zero failures, but at a 13% rate that outcome has a 42% chance of happening anyway. The cheap improvement is having that test assert `capturedEvents()` alongside the path, so the next occurrence names its own cause instead of only its symptom; the reason it cannot today is that `beforeEach` resets captured events and the failing case never reads them, so every occurrence throws away its own explanation. **The full logs lived in a scratchpad directory that does not survive the session, so this entry is all that is left of them.** **Six more full suite runs on 2026-09-10, during the v1 audit, were all green.** That proves nothing on its own: at 13%, six clean runs in a row happen about 43% of the time. The running total is 2 failures in 21 full suite runs, and the `capturedEvents()` assertion is still not added.
- **The production run of the core loop is recorded in no `verify.md` yet.** The engineer drove the whole loop on `usejobhunt.dev` on 2026-09-10 with a real account: a profile, a search, ranked results with the reasoning shown, and two real applications recorded (one `good_match`, one `strong_match`). It is written up in `docs/overview.md` (pull request 122, merged) and in the resolution note of the v1 readiness audit. **Corrected 2026-09-11: that audit is no longer in the repository.** Pull request 127 removed it, and it now lives outside the repo in `../jobhunt-notes/`, beside `project-story.md`. It stays readable in git history, where pull request 122 added it. It also bears on spec 0019's `verify.md`: its second descoped step (`COPY-9` and `COPY-11` rendering on a real page) is now visible in `docs/images/results-list.png`, word for word against `src/features/scoring/copy.ts:134` and `:168`, while its first descoped step (the pending list, then one reorder) appears in no screenshot. `/check verify` owns that file, so this skill leaves it alone. Still unchecked: whether Sentry's `production` environment received `usage_gate.check` spans from that run, which would give the production monitors their first real denominator.
- **Whether v1 includes features 18, 19 and 20 is undecided, and it is now the only thing keeping "v1 complete" from being true.** All three are still `planned` in Slice 3 and Slice 4, above the `## v1.5` heading in `docs/scope/scope.md`. Recommended on 2026-09-11 and not yet acted on: move 18 and 20 to v1.5 through `/scope` with the reason on record, since the completion test in the scope's standing rules needs neither, and run `/architect` on 19, whose fake salary range ("$109,440 to $109,440") is visible in the README's results list screenshot. **The brief contradicts itself on this, recorded 2026-09-11.** Its completion test (`docs/archive/jobhunt-idea-brief.md` line 31) says v1 is the loop and anything beyond it is v1.5, which is met today, but the same brief's v1 summary (line 177) lists the structured search filters and guided capture as v1. Which half the scope follows is a `/scope` decision.
- **The escalating `database_unavailable` issue on production has no open item of its own.** Only one hypothesis about it is recorded, under Ruled out below, and nothing says the issue itself was resolved. The v1 audit (2026-09-10, section 11) flagged it as the only production side error it found any trace of. Worth a look in Sentry.
- **Presentation plan, in order, recorded 2026-09-11 from the advisor chat.** (1) The usage indicator, feature 28, through `/architect` first: its scope row carries only a `Build it` box and no design step, which is wrong, because a normal session cannot read the usage tables (both carry row level security with no policies). (2) A salary display fix taken from feature 19, done before the demo: one figure when the minimum equals the maximum, and no estimate that contradicts a salary the posting states. It comes first because the demo shows the same Adzuna data, and the bug is visible in the README's results list screenshot ("$109,440 to $109,440 (estimated)" on a card whose excerpt states $116,127 to $157,119). (3) The demo route: read only, with scored results prepared in advance and no sign in, `/architect` first; the nearest existing scope row is feature 31, "Seeded demo account", in v1.5. Also pending: the interview quiz before the first interview; fixing monitoring (the primary uptime monitor watches a cached page, the second cannot tell a redirect from a working page, and 2 of 23 named spans have an alert); then a README section on the error model and binding rules, only after monitoring is fixed.
- **An ESLint ban on `auth.getSession` under `src/` is recorded only in pull request 123's description.** Root `AGENTS.md` now states the rule ("Verify the caller with `getClaims()`, never `getSession()`", added by `635ab4e`), but only review enforces it: `eslint.config.mjs` has no such ban (checked 2026-09-11).
- **Optional: rename merged pull request 122, the one that added the v1 readiness audit, and trim the descriptions of 122 and 125**, so the audit, now private, is not signposted. The diffs stay readable either way.

- **Reflex candidate, the UTC day roll, now three instances and two of them on 2026-09-09.** A date keyed counter re-read returned zero rows because the UTC day had rolled while local time had not (local is UTC minus 4, so the day rolls at 20:00 local), and separately a note advised watching a specific day's row for movement that would have shown none for the same reason. The third is the older one already written into the standing instruction below. **The rule to write**: query a date keyed counter by explicit date and print the row count, because a rolled period returns zero rows and zero rows reads exactly like zero calls. Not yet in `docs/reflexes.md`.
- **Reflex candidate, `console.log` swallowed by the Vitest reporter, which lost a paid measurement run whole.** On 2026-09-09 a five call live latency run against a real vendor completed, passed, and discarded its entire deliverable, because this project's integration runs do not surface `console.log` to the reporter at all and nothing in `vitest.config.mts` or the setup files suppresses it deliberately. Proved with a free probe on a passing test that made no vendor call. The money had to be spent a second time to get the numbers back. **The rule to write**: a measurement that costs money must be written to a file, incrementally, before the next paid call is made, never printed and never written only after the loop. Not yet in `docs/reflexes.md`.
- **Reflex candidate, `git checkout --` on an untracked file silently does nothing and exits clean.** On 2026-09-09 a deliberate test mutation was reverted that way, the command reported success, and the break stayed in the file; it was caught only by a later grep. **The rule to write**: after reverting a deliberate break, confirm the revert landed rather than trusting the exit code, because an untracked path is not an error to `git checkout`. **Explicitly NOT a new rule**: `git stash drop` destroying unrelated uncommitted work, which happened twice in the same session and cost three scope ticks and 24 `verify.md` step states (recovered from an unreachable stash commit). The 2026-08-23 reflex already covers reading the full `git status` before any command that discards working tree state, and it simply was not followed. Not yet in `docs/reflexes.md`.

## Ruled out

- **Switching off Supabase's Data API for about a minute**, on a project that was not identified afterward, considered as the cause of the escalating `database_unavailable` issue on production. Rejected: the specific error, a Postgres grant hint naming the `anon` role on one table, does not match what a disabled Data API would produce, and the timing does not clearly line up.

- **A suspected spec 0011 defect, that per account usage counters are never written.** Raised during `/check verify` on 2026-09-07: `usage_gate_counter` held only `scope=global` rows while `usage_cap` defines `job_search account week` and `ai_scoring account week`, which looked like two caps that could never be enforced. Disproved the same session. The account scoped rows are created on the first account scoped call of the period, and once enough calls landed they read `job_search account week` 26 attempts against a cap of 25 and the gate correctly refused the next search with feature 10's own sentence. The earlier reading was simply taken before any account scoped call had happened in that period. The gate works; do not re-investigate.

## Standing instructions

- The Vercel CLI **is** usable in this environment (version 59.3.0, project already linked), and `vercel env ls`, `vercel env pull`, `vercel inspect` and `vercel project inspect` all worked on 2026-08-22. Corrects an earlier note here saying nothing could read Vercel state. Two limits remain: the Vercel MCP server is still unauthorised, and a variable marked Sensitive (both `SUPABASE_SECRET_KEY` values, `SENTRY_AUTH_TOKEN`) pulls as `[SENSITIVE]`, so its value can never be compared or exercised from here. Ask the engineer for anything behind those two limits. **`vercel inspect` does not print a deployment's git commit, but the deployments API does**: on 2026-09-10, `https://api.vercel.com/v13/deployments/<id>` called with the CLI's own saved token returned `meta.githubCommitSha`, which is how the v1 audit proved production was serving `origin/main`.
- The permission classifier that can block a merge does not fire every time. On 2026-08-23, `gh pr merge` was blocked once, for PR 5, and the engineer had to run it by hand. The very same command then went through with no block at all for PR 6, PR 7, and PR 8, and a `vercel promote` call also went through unblocked. So a merge or a promote may or may not be stopped, there is no way to know in advance, and the block is not something to route around if it does fire. The sharpest evidence came on 2026-08-26: `gh pr merge 14` went through and merged, then the same command shape for PR 15, seconds later in the same session, was blocked. A `git push` and a `gh pr create` for PR 12 went through unblocked that day too, and the engineer merged PR 12 and PR 15 by hand. So the block does not track the command, the repository, or the session; treat it as genuinely unpredictable. Keep asking the engineer to confirm before every merge exactly as AGENTS.md's Git section already requires, whether or not the classifier ends up blocking that particular attempt. Confirmed again on 2026-08-27 with a new wrinkle worth knowing: the classifier blocked `git filter-branch`, then blocked a plainly read only `git log --reverse` moments later in the same session, while `git push`, `gh pr merge` for pull requests 19 and 20, `git cherry-pick`, `git branch -D` and a remote branch delete all went through unblocked. So it can block reads too once it is in that mode. The response that worked was handing the engineer the exact commands to run, not retrying. On 2026-08-30 it fired on nothing at all: two pushes, two `gh pr create` calls and two `gh pr merge` calls all went through unblocked, which is more evidence that it does not track the command. **It is not only about merges, confirmed 2026-09-07.** During `/check verify` it refused, in a row, reading key prefixes out of `.env.local`, writing a JavaScript file that embedded a minted session token, and starting a local HTTP server on a scratch port. Three different shapes, all local, none destructive. That cost the whole browser sweep for feature 14, which was reported BLOCKED rather than worked around. The response that worked was the one already recorded here: hand the engineer the commands. Do not try a fourth variant of the same goal.
- **The local weekly search budget is spent for the week of 2026-09-07, by verification rather than by use.** `job_search account week` stands at 44 attempts against a cap of 25, so `/search` answers with the allowance sentence rather than results until the week rolls over. Nothing is broken and no code changed. To run another `/check verify` pass before then, raise the `usage_cap` row locally and put it back to 25 afterwards; `ai_scoring account week` is also over, and both were raised and restored again during the 2026-09-07 verification work. **Read as of the end of 2026-09-07**: `job_search account week` 58 of 25, `ai_scoring account week` 815 of 500, `ai_scoring global day` 999 of 1320. Both account caps sit at their committed values (25 and 500); the overage is spent attempts, not a raised cap left behind. **Read again at the end of 2026-09-08**, through `pg` directly since `usage_gate_counter` carries no row level security policy and PostgREST cannot see it: `ai_scoring global day` 195 of 1320 for 2026-09-08 (the day counter resets, so yesterday's 1002 is history), `ai_scoring global month` 1225 of 40000, `job_search account week` still 58 of 25, and `ai_scoring account week` still **815** of 500. That last number is the useful one: about 195 real `ai_scoring` calls were spent today by feature 16's eval harness and the account week counter did not move at all, because the harness mints a fresh fixture user every run. That is spec 0017's Follow up claim confirmed by measurement rather than by reading the code, and it is why the global day cap of 1320, not the account cap of 500, is the ceiling that actually binds a `pnpm eval` run. `psql` is **not** installed on this machine; use a short node script importing `pg` from the repo root. **Read again at the end of 2026-09-09**, through `pg` directly: `ai_scoring global day` is 5 of 1320 for 2026-09-09 (the five calls feature 33's confirming run spent), `global month` 1230 of 40000, `account week` still 815 of 500 and `job_search account week` still 58 of 25. Both account caps stay at their committed values. Note the UTC day rolls at 20:00 local, so a late evening run lands on the next day's row: watching the previous day's number would show no movement and read as though nothing ran.
